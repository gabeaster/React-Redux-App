import React from 'react';
//import useEffect to fetch house at top of CharacterList
//import { connect } from 'react-redux;
//import Loader from 'react-loader-spinner';
//import fetch action 

const CharacterList = () => {
    //create useEffect to fetch house

    //create isFetching with loader

    return (
        <div className='listContainer'>
            {/* use js to display <Loader /> while fetching */}
            {/* <h2>PUT NAME OF HOUSE FROM PROPS HERE</h2> */}
            {/* use js to display error */}

        </div>
    )
};

export default CharacterList;
//setup mapStateToProps and connect